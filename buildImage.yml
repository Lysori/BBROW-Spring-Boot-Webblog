---
  - name: Build Dummy nginx Image as test scenario
    hosts: localhost
    tasks:  
    - name: Remove image
      docker_image:
        state: absent
        name: bbrow/nginxtest
        tag: v3

    - name: Build dummy nginx image
      docker_image:
        name: bbrow/nginxtest
        build:
         pull: no
         path: testdockerfiles/
        tag: v3
        source: build